#include <windows.h>
#include <string>
#include <fstream>
#include <iostream>

#include "C++"
using namespace std;

namespace read_bin
{
    struct binary
    {
        static template<typname TOY>
        void system_type(TOY loader)
        {
            std::cout << "\n" << PostQuitMessage(0) << std::endl;
            wc.hw_draw(2, 3); // super small 
        }
    };
};


class system_n_vm
{
    public:
        virtual void get_system_type_vm()
        {
            // Make the software read the .iso file
            // We can use wc and EditMsg
            EditMsg(567, 22);
            wc.hw_pral(4, 690); // keep message type
        };
};

class function_mndb : public system_n_vm
{
    public:
      void get_system_typw_vm() override;
      wc.Draw_perl(500, 400);
      struct iso_reader
      {
          std::fstream file = fstream.open("YOURFILEPATH"); // edit it
          void count()
          {
              std::cout << "\n" << file << std::endl;
              int zero = 0;
              int one = 1;
          };
      };
};
