#include <windows.h>
#include <string>
#include <fstream>
#include <iostream>
using namespace std;

class filetype
{
	public:
		virtual void local_filetype(string type) { _System_error_message("Please type the type message"); };
		virtual void Symphony_type()
		{
			PostQuitMessage(0);
		};
};

class driverded_type : public filetype
{
	public:
		void local_file(string iso) override;
};

namespace GETISODATA
{
	void system_data()
	{
		string data_refrence = fstream.open("C:\\Users\\Desktop\\Ocale NVMware\\VirtualMachineType_iso\\An Normal Iso.iso");
		data_refrence.GetData();
	};
};

namespace MEMORYSETTINGS
{
	template<typename VOID>
	static void system_data32(VOID loader)
	{
		// Lets create a local Data system
		GETISODATA isodata;
	};

	// Memory
	std::cout << "\n" << std::endl; 
	// Memory type
	std::fstream iso = fstream.open("C:\\Users\\Desktop\\Ocale NVMware\\VirtualMachineType_iso\\An Normal Iso.iso"); // gets the iso
	// The following class can be overrdieable
	class memory
	{
		public:
			virtual void system_file_loader_memory(string type_path_filetype)
			{
				// Lets get the data int -> dockerdata 
				struct dockerdata
				{
					// we can create a dockerdata, for example we can do, find the data, lets create a function, to get the data
					void get_dockerdata(string path)
					{
						std::fstream dockerdatafile = fstream.open("C:\\Users\\Desktop\\Ocale NVMware\\"); //  let the user change it by themselves 
						dockerdatafile.app();
						
						// Print the user path + Say the user to change it, if they did not
						std::cout << "The path is: " << path << ", ERROR(for best practice): Please check in the C++ scrips(mainly in the VirtulaMachineType_iso folder), and add your user name: C:\\User\\YourUserName\\Dekstop\\Ocale NVMware\\An Normal Iso.iso . Please fix and check the errors" << std::endl;

					};

					int dockerdata;
				};

				dockerdata.dockerdata.dockerdata = 32; // depending on the lunes
			};
	};

	class driverded2 : public memory
	{
		public:
			void system_file_loader_memory(int path_characters) override;
	};
};

namespace GetFileType
{
	// Refrence sheet:
	fstream.open("C:\\Users\\Desktop\\Ocale NVMware\\VirtualMachineType_iso\\An Normal Iso.iso"); // create the reference

	void GET_ISO_DATA()
	{
		GETISODATA isodata;
		int local_type = isodata.system_data();
	};

	// WIN32 API
	int WINAPI CreateWindow(int size, string header, int height)
	{
		// Templates
		static template<typename Toy> // creates a static template
		void loader_init_ps1()
		{
			GETISODATA isodata;
			Toy system_type = isodata.system_data();
		};
	};

	void system_line() { MEMORYSETTINGS memory; memory.system_data32("all the 32 nit applications %%") };
};

// Make the validation system, where it can check if it d a data, we will use the Reference sheet

namespace GetIsoData
{
	GetFileType FT;
	void system()
	{

		string inputpt;
		cin >> inputpt;
		fstream.open(inputpt); // user path indecremisis
	};
};

// virtual function -> WIN32 API (call the main system)

int main()
{
	GetFileType.GET_ISO_DATA();
	GetIsoDta.system(); // call the function
	// done
};
// end of file[eof]